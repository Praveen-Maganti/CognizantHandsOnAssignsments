
<list> -> List represents a java list which internally creates new ArrayList<>()
<ref>  -> It references an existing Spring-managed bean by its id instead of creating a new object.
    bean="id" -> It specifies the ID of the bean to be injected into the collection.

Internally it creates all the objects and add those to the ArrayList